name: CI/CD Pipeline

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  # Backend Build & Deploy
  backend:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    # Setup Node.js for Backend
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    # Install backend dependencies
    - name: Install dependencies (Backend)
      working-directory: ./backend
      run: |
        npm install

    # Backend Docker Build
    - name: Build Backend Docker image
      run: |
        docker build -t my-project-backend ./backend

    # Login to Docker Hub
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # Tag Backend Docker image for Docker Hub
    - name: Tag Backend Docker image for Docker Hub
      run: |
        docker tag my-project-backend docker.io/akash3103/my-project-backend:latest

    # Push Backend Docker image to Docker Hub
    - name: Push Backend Docker image to Docker Hub
      run: |
        docker push docker.io/akash3103/my-project-backend:latest
    # Deploy to Kubernetes using kubectl
    - name: Set up Kubernetes
      run: |
        kubectl apply -f k8s/backend-deployment.yaml --validate=false
        kubectl apply -f k8s/backend-service.yaml --validate=false

  # Frontend Build & Deploy
  frontend:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    # Setup Node.js for Frontend
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    # Install frontend dependencies
    - name: Install dependencies (Frontend)
      working-directory: ./frontend
      run: |
        npm install

    # Build Frontend
    - name: Build Frontend
      working-directory: ./frontend
      run: |
        npm run build

    # Frontend Docker Build
    - name: Build Frontend Docker image
      run: |
        docker build -t my-project-frontend ./frontend

    # Login to Docker Hub
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # Tag Frontend Docker image for Docker Hub
    - name: Tag Frontend Docker image for Docker Hub
      run: |
        docker tag my-project-frontend docker.io/akash3103/my-project-frontend:latest

    # Push Frontend Docker image to Docker Hub
    - name: Push Frontend Docker image to Docker Hub
      run: |
        docker push docker.io/akash3103/my-project-frontend:latest

    # Deploy to Kubernetes using kubectl
    - name: Set up Kubernetes
      run: |
        kubectl apply -f k8s/frontend-deployment.yaml --validate=false
        kubectl apply -f k8s/frontend-service.yaml --validate=false

  # Clean up after deployment
  cleanup:
    runs-on: ubuntu-latest
    needs: [backend, frontend]

    steps:
    - name: Clean up Docker images
      run: |
        docker system prune -f
